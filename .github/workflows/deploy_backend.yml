name: Deploy to Production (Legacy)

on:
  push:
    branches:
      - production

jobs:
  trigger-build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Trigger Build and Test workflow
        run: |
          echo "This workflow is deprecated."
          echo "The new workflow structure is:"
          echo "1. Build and Test (.github/workflows/build.yml)"
          echo "2. Deploy Database (.github/workflows/deploy-database.yml)"
          echo "3. Deploy Application (.github/workflows/deploy-application.yml)"
          echo ""
          echo "The Build and Test workflow will be triggered automatically on push to production."
          echo "Please use the new workflow structure for better separation of concerns."
